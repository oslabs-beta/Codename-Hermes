{
  "name": "Codename_Hermes",
  "packageManager": "yarn@3.4.1",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "start": "yarn workspaces foreach -vpti run start",
    "start:dev": "yarn workspaces foreach -vpti run start:dev",
    "build": "yarn workspaces foreach -vpti run build"
  }
}
