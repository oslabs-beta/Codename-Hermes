{
  "name": "library",
  "packageManager": "yarn@3.3.1",
  "main": "./lib/index.js",
  "scripts": {
    "build": "yarn tsc",
    "build:install": "yarn build && yarn add ./"
  },
  "devDependencies": {
    "tsc": "^2.0.4",
    "typescript": "^4.9.5"
  },
  "dependencies": {
    "kafka-node": "^5.0.0"
  }
}
